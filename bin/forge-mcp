#!/bin/sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
APP_EXEC="$SCRIPT_DIR/../../MacOS/Forge"
MCP_SCRIPT="$SCRIPT_DIR/../scripts/forge-mcp.mjs"

exec env ELECTRON_RUN_AS_NODE=1 "$APP_EXEC" "$MCP_SCRIPT" "$@"
