#!/bin/sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
APP_EXEC="$SCRIPT_DIR/../../MacOS/Forge"
PUBLISH_SCRIPT="$SCRIPT_DIR/../scripts/forge-publish.mjs"

exec env ELECTRON_RUN_AS_NODE=1 "$APP_EXEC" "$PUBLISH_SCRIPT" "$@"
